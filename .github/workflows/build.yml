name: build
on: [push]

jobs:
  build-main:
    runs-on: self-hosted
    container:
      image: adnrv/texlive:adntools
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Compile PDF
        run: |
          latexmk -pdf --shell-escape %O %S "report.tex"
